# 🧪 Все термины по тестированию

```dataview
TABLE русский as "На русском", file.tags as "Теги"
FROM "03-Learning/English/Terms/"
WHERE contains(file.tags, "#тестирование")
SORT file.name
```

## 📈 Статистика
- **Всего терминов:** `=` length(rows)
- **Изучено:** `=` length(filter(rows, (r) => contains(r.теги, "#изучено")))
- **В процессе:** `=` length(filter(rows, (r) => contains(r.теги, "#в_процессе")))

## 🎯 Приоритеты изучения
1. [[Automated Testing]] - **В процессе** (начато {{date:DD.MM.YYYY}})
2. [[Selenium]] - **Запланировано**
3. [[Continuous Integration (CI)]] - **Запланировано**

---
[[Вернуться в главный словарь|IT Dictionary]]